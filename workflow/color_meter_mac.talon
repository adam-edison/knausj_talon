os: mac
-

# Switch to Classic Color Meter app, press Shift-Cmd-C, then notify.
# If the app isn't running you'll get a notification; add its exact name to
# core/app_switcher/app_name_overrides.mac.csv if "focus <name>" uses something different.
copy color: user.copy_color()
