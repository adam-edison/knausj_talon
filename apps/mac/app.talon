os: mac
-
(alfred | spotlight):
    key(cmd-space)
    
key(cmd-f11):
    mode.disable("sleep")
    mode.disable("user.gaming")
    mode.disable("dictation")
    mode.disable("command")
    speech.disable()
